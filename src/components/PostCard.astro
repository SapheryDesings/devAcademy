---
const { titulo, descripcion, fecha, slug } = Astro.props;
---

<article class="post-card">
  <h2>{titulo}</h2>
  <p class="post-date">{fecha}</p>
  <p>{descripcion}</p>
  <a href={`/devAcademy/rutas/${slug}/`} class="read-more">Leer más..</a>
</article>

<style>
  .post-card {
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 16px;
    background: #fff;
    color: #1e293b;
    box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s ease-in-out;
  }
  .post-card:hover {
    transform: scale(1.02);
  }
  .post-date {
    color: #1e293b60;
    font-size: 0.9rem;
  }
  .read-more {
    display: inline-block;
    margin-top: 10px;
    color: #0ea5e9;
    text-decoration: underline;
  }
  .read-more:hover {
    color: #f97316;
  }
</style>
